version: 2

seeds:
  - name: divisions
    config:
     schema: "{{ env_var('DBT_SEEDSCHEMA', 'SEEDDATA') }}"  
  - name: categories
    config:
     schema: "{{ env_var('DBT_SEEDSCHEMA', 'SEEDDATA') }}"
  - name: shippers
    config:
     schema: "{{ env_var('DBT_SEEDSCHEMA', 'SEEDDATA') }}"